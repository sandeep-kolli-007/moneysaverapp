<resources>
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Make sure there is no windowBackground property pointing to a drawable or logo -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>
 <style name="BootTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- enforce status bar style as the example app doesn't support dark mode -->
        <!-- <item name="darkContentBarsStyle">true</item>-->
        <item name="bootSplashBackground">@color/bootsplash_background</item> 
        <item name="android:windowBackground">@drawable/boot</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <item name="bootSplashBrand">@drawable/bootsplash_logo</item>
        <!-- <item name="postBootSplashTheme">@style/AppTheme</item> -->
    </style>
</resources>
