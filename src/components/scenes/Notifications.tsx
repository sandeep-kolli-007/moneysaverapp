import { View, Text } from 'react-native'
import React from 'react'
import Layout from '../shared/Layout'

const Notifications = () => {
  return (
    <Layout title={'Noti'} isBackEnabled bottomsheet={<></>}>
 
    </Layout>
  )
}

export default Notifications